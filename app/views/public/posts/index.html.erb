<div class="container-fluid">
  <div class="row">
    <%= render 'layouts/sidebar' %>
    <div class="col-md-8">
      <%= render 'index', posts: @posts %>
      <%= paginate @posts %>
    </div>
  </div>
</div>