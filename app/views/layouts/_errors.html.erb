<% if obj.errors.any? %>
  <div class="row mx-auto">
    <div id="error_explanation">
      <h3 class="text-danger"><%= obj.errors.count %> 件のエラーが発生しました</h3>
      <ul class="text-danger">
        <% obj.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  </div>
<% end %>